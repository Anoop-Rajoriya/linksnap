<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LinkSnap - Login</title>
    <link rel="stylesheet" href="/base.css" />
  </head>
  <body>
    <%- include("../partials/header") %>
    <main class="login">
      <% if(messages && messages.success){ %>
      <p class="alert success">Success: <%= messages.success %></p>
      <% } %> <% if(messages && messages.error){ %>
      <p class="alert error">Error: <%= messages.error %></p>
      <% } %>
      <form action="" class="form-card">
        <h2 class="form-heading">Login your account</h2>
        <div class="email input-field">
          <label for="email">Email *</label>
          <input
            type="email"
            name="email"
            id="email"
            required
            placeholder="Your Email..."
          />
        </div>
        <div class="password input-field">
          <label for="password">Password *</label>
          <input
            type="text"
            name="password"
            id="password"
            required
            placeholder="Your Password..."
          />
        </div>
        <button class="form-button">Login</button>
        <p class="note">
          If you don't have account <a href="/register">register</a>.
        </p>
      </form>
    </main>
    <%- include("../partials/footer") %>
  </body>
</html>
