<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LinkSnap | <%= title %></title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <header>
      <a href="/">LinkSnap</a>
      <nav>
        <% if(routes && routes.length){ routes.forEach(function(route){ %>
        <a href="<%= route.path %>"><%= route.name %></a>
        <% })} %>
      </nav>
    </header>
    <main>
