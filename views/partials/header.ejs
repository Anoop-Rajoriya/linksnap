<header>
  <a class="logo" href="/">LinkSnap <%= "| " + route %></a>
  <nav>
    <% if (routes && routes.length > 0) { routes.forEach(function(route) { %>
    <a class="link" href="<%= route.path %>"><%= route.name %></a>
    <% })} %>
  </nav>
</header>
