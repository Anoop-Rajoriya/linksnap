<header>
  <a href="/" class="logo-link">LinkSnap <%= "| " + route %></a>
  <nav>
    <% if (routes && routes.length > 0) { %> <% routes.forEach(function(route) {
    %>
    <a class="links" href="<%= route.path %>"><%= route.name %></a>
    <% }); %> <% } %>
  </nav>
</header>
